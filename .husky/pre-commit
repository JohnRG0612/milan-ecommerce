#!/usr/bin/env sh
# Bloquea commits cuando lint o tests fallen.
# Activado en Fase B después de cerrar el ciclo TDD del buscador
# (los commits "rojo" se hicieron antes de esta activación).

pnpm lint
pnpm test
